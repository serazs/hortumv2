local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local SoundService = game:GetService("SoundService")
local StarterGui = game:GetService("StarterGui")
local TextChatService = game:GetService("TextChatService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

local Folder = Instance.new("Folder", Workspace)
local Part = Instance.new("Part", Folder)
local Attachment1 = Instance.new("Attachment", Part)
Part.Anchored = true
Part.CanCollide = false
Part.Transparency = 1

if not getgenv().Network then
    getgenv().Network = {
        BaseParts = {},
        Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)
    }

    Network.RetainPart = function(Part)
        if typeof(Part) == "Instance" and Part:IsA("BasePart") and Part:IsDescendantOf(Workspace) then
            table.insert(Network.BaseParts, Part)
            Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
            Part.CanCollide = false
        end
    end

    local function EnablePartControl()
        LocalPlayer.ReplicationFocus = Workspace
        RunService.Heartbeat:Connect(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            for _, Part in pairs(Network.BaseParts) do
                if Part:IsDescendantOf(Workspace) then
                    Part.Velocity = Network.Velocity
                end
            end
        end)
    end

    EnablePartControl()
end

local function ForcePart(v)
    if v:IsA("Part") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
        for _, x in next, v:GetChildren() do
            if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then
                x:Destroy()
            end
        end
        if v:FindFirstChild("Attachment") then
            v:FindFirstChild("Attachment"):Destroy()
        end
        if v:FindFirstChild("AlignPosition") then
            v:FindFirstChild("AlignPosition"):Destroy()
        end
        if v:FindFirstChild("Torque") then
            v:FindFirstChild("Torque"):Destroy()
        end
        v.CanCollide = false
        local Torque = Instance.new("Torque", v)
        Torque.Torque = Vector3.new(100000, 100000, 100000)
        local AlignPosition = Instance.new("AlignPosition", v)
        local Attachment2 = Instance.new("Attachment", v)
        Torque.Attachment0 = Attachment2
        AlignPosition.MaxForce = 9999999999999999
        AlignPosition.MaxVelocity = math.huge
        AlignPosition.Responsiveness = 200
        AlignPosition.Attachment0 = Attachment2
        AlignPosition.Attachment1 = Attachment1
    end
end

--------------------------------------------------
-- SOUND
--------------------------------------------------
local function playSound(id)
    local s = Instance.new("Sound")
    s.SoundId = "rbxassetid://"..id
    s.Parent = SoundService
    s:Play()
    s.Ended:Connect(function()
        s:Destroy()
    end)
end

playSound("2865227271")

--------------------------------------------------
-- GUI (RGB BORDER, RESETTE KALICI)
--------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SuperRingPartsGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- RGB BORDER FRAME
local Border = Instance.new("Frame")
Border.Size = UDim2.new(0,320,0,280)
Border.Position = UDim2.new(0.5,-160,0.5,-140)
Border.BorderSizePixel = 0
Border.Parent = ScreenGui

-- İÇ FRAME
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(1,-4,1,-4)
MainFrame.Position = UDim2.new(0,2,0,2)
MainFrame.BackgroundColor3 = Color3.fromRGB(0,0,0)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = Border

-- RGB LOOP
task.spawn(function()
    local h = 0
    while true do
        h += 0.01
        if h > 1 then h = 0 end
        Border.BackgroundColor3 = Color3.fromHSV(h,1,1)
        task.wait(0.03)
    end
end)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,40)
Title.Text = "Super Ring Parts"
Title.TextColor3 = Color3.new(1,1,1)
Title.BackgroundColor3 = Color3.fromRGB(20,20,20)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.Parent = MainFrame

local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0.8,0,0,35)
ToggleButton.Position = UDim2.new(0.1,0,0.12,0)
ToggleButton.Text = "Ring Parts Off"
ToggleButton.BackgroundColor3 = Color3.fromRGB(30,30,30)
ToggleButton.TextColor3 = Color3.new(1,1,1)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 16
ToggleButton.Parent = MainFrame

local DecreaseRadius = Instance.new("TextButton")
DecreaseRadius.Size = UDim2.new(0.15,0,0,35)
DecreaseRadius.Position = UDim2.new(0.1,0,0.3,0)
DecreaseRadius.Text = "<"
DecreaseRadius.BackgroundColor3 = Color3.fromRGB(30,30,30)
DecreaseRadius.TextColor3 = Color3.new(1,1,1)
DecreaseRadius.Parent = MainFrame

local IncreaseRadius = Instance.new("TextButton")
IncreaseRadius.Size = UDim2.new(0.15,0,0,35)
IncreaseRadius.Position = UDim2.new(0.75,0,0.3,0)
IncreaseRadius.Text = ">"
IncreaseRadius.BackgroundColor3 = Color3.fromRGB(30,30,30)
IncreaseRadius.TextColor3 = Color3.new(1,1,1)
IncreaseRadius.Parent = MainFrame

local RadiusDisplay = Instance.new("TextLabel")
RadiusDisplay.Size = UDim2.new(0.4,0,0,35)
RadiusDisplay.Position = UDim2.new(0.3,0,0.3,0)
RadiusDisplay.Text = "Radius: 50"
RadiusDisplay.BackgroundColor3 = Color3.fromRGB(20,20,20)
RadiusDisplay.TextColor3 = Color3.new(1,1,1)
RadiusDisplay.Parent = MainFrame

-- MOD BUTONLARI (Tornado, Heart)
local TornadoBtn = Instance.new("TextButton")
TornadoBtn.Size = UDim2.new(0.3,0,0,30)
TornadoBtn.Position = UDim2.new(0.05,0,0.48,0)
TornadoBtn.Text = "Tornado"
TornadoBtn.BackgroundColor3 = Color3.fromRGB(50,205,50) -- aktif başlangıç
TornadoBtn.TextColor3 = Color3.new(1,1,1)
TornadoBtn.Font = Enum.Font.GothamBold
TornadoBtn.TextSize = 14
TornadoBtn.Parent = MainFrame

local HeartBtn = Instance.new("TextButton")
HeartBtn.Size = UDim2.new(0.3,0,0,30)
HeartBtn.Position = UDim2.new(0.37,0,0.48,0)
HeartBtn.Text = "Heart"
HeartBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
HeartBtn.TextColor3 = Color3.new(1,1,1)
HeartBtn.Font = Enum.Font.GothamBold
HeartBtn.TextSize = 14
HeartBtn.Parent = MainFrame

-- Tornado alt modları (H ve V)
local TornadoH = Instance.new("TextButton")
TornadoH.Size = UDim2.new(0.1,0,0,30)
TornadoH.Position = UDim2.new(0.69,0,0.48,0)
TornadoH.Text = "H"
TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
TornadoH.TextColor3 = Color3.new(1,1,1)
TornadoH.Font = Enum.Font.GothamBold
TornadoH.TextSize = 16
TornadoH.Parent = MainFrame

local TornadoV = Instance.new("TextButton")
TornadoV.Size = UDim2.new(0.1,0,0,30)
TornadoV.Position = UDim2.new(0.8,0,0.48,0)
TornadoV.Text = "V"
TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
TornadoV.TextColor3 = Color3.new(1,1,1)
TornadoV.Font = Enum.Font.GothamBold
TornadoV.TextSize = 16
TornadoV.Parent = MainFrame

-- Heart alt modları (H ve V)
local HeartH = Instance.new("TextButton")
HeartH.Size = UDim2.new(0.1,0,0,30)
HeartH.Position = UDim2.new(0.69,0,0.58,0)
HeartH.Text = "H"
HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
HeartH.TextColor3 = Color3.new(1,1,1)
HeartH.Font = Enum.Font.GothamBold
HeartH.TextSize = 16
HeartH.Parent = MainFrame

local HeartV = Instance.new("TextButton")
HeartV.Size = UDim2.new(0.1,0,0,30)
HeartV.Position = UDim2.new(0.8,0,0.58,0)
HeartV.Text = "V"
HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
HeartV.TextColor3 = Color3.new(1,1,1)
HeartV.Font = Enum.Font.GothamBold
HeartV.TextSize = 16
HeartV.Parent = MainFrame

-- Watermark
local Watermark = Instance.new("TextLabel")
Watermark.Size = UDim2.new(1,0,0,20)
Watermark.Position = UDim2.new(0,0,1,-30)
Watermark.Text = "Super Ring"
Watermark.TextColor3 = Color3.fromRGB(255,255,255)
Watermark.BackgroundTransparency = 1
Watermark.Font = Enum.Font.Gotham
Watermark.TextSize = 12
Watermark.Parent = MainFrame

-- Minimize button
local MinimizeButton = Instance.new("TextButton")
MinimizeButton.Size = UDim2.new(0,25,0,25)
MinimizeButton.Position = UDim2.new(1,-30,0,5)
MinimizeButton.Text = "-"
MinimizeButton.BackgroundColor3 = Color3.fromRGB(30,30,30)
MinimizeButton.TextColor3 = Color3.new(1,1,1)
MinimizeButton.Font = Enum.Font.GothamBold
MinimizeButton.TextSize = 16
MinimizeButton.Parent = MainFrame

local minimized = false
MinimizeButton.MouseButton1Click:Connect(function()
    minimized = not minimized
    if minimized then
        Border:TweenSize(UDim2.new(0,320,0,40), "Out", "Quad", 0.3, true)
        MinimizeButton.Text = "+"
        ToggleButton.Visible = false
        DecreaseRadius.Visible = false
        IncreaseRadius.Visible = false
        RadiusDisplay.Visible = false
        TornadoBtn.Visible = false
        HeartBtn.Visible = false
        TornadoH.Visible = false
        TornadoV.Visible = false
        HeartH.Visible = false
        HeartV.Visible = false
        Watermark.Visible = false
        Title.Visible = false
    else
        Border:TweenSize(UDim2.new(0,320,0,280), "Out", "Quad", 0.3, true)
        MinimizeButton.Text = "-"
        ToggleButton.Visible = true
        DecreaseRadius.Visible = true
        IncreaseRadius.Visible = true
        RadiusDisplay.Visible = true
        TornadoBtn.Visible = true
        HeartBtn.Visible = true
        TornadoH.Visible = true
        TornadoV.Visible = true
        HeartH.Visible = true
        HeartV.Visible = true
        Watermark.Visible = true
        Title.Visible = true
    end
    playSound("12221967")
end)

-- Draggable
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    Border.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

Border.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Border.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Border.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

--------------------------------------------------
-- RING PARTS LOGIC
--------------------------------------------------
local radius = 50
local height = 100
local rotationSpeed = 1
local attractionStrength = 1000
local ringPartsEnabled = false
local currentMode = "tornado" -- "tornado" veya "heart"

-- Her mod için H/V durumları
local tornadoMode = "V" -- varsayılan tornado V (dikey salınımlı)
local heartMode = "H"   -- varsayılan heart H (düz)

local partAngles = {}

local function RetainPart(Part)
    if Part:IsA("BasePart") and not Part.Anchored and Part:IsDescendantOf(Workspace) then
        if Part.Parent == LocalPlayer.Character or Part:IsDescendantOf(LocalPlayer.Character) then
            return false
        end
        Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
        Part.CanCollide = false
        return true
    end
    return false
end

local parts = {}
local function addPart(part)
    if RetainPart(part) then
        if not table.find(parts, part) then
            table.insert(parts, part)
            partAngles[part] = math.random() * 2 * math.pi
        end
    end
end

local function removePart(part)
    local index = table.find(parts, part)
    if index then
        table.remove(parts, index)
        partAngles[part] = nil
    end
end

for _, part in pairs(Workspace:GetDescendants()) do
    addPart(part)
end

Workspace.DescendantAdded:Connect(addPart)
Workspace.DescendantRemoving:Connect(removePart)

-- Tornado güncellemesi (H ve V seçeneğine göre)
local function tornadoUpdate()
    local humanoidRootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if humanoidRootPart then
        local center = humanoidRootPart.Position
        for _, part in pairs(parts) do
            if part.Parent and not part.Anchored then
                local pos = part.Position
                local distance = (Vector3.new(pos.X, center.Y, pos.Z) - center).Magnitude
                local angle = math.atan2(pos.Z - center.Z, pos.X - center.X)
                local newAngle = angle + math.rad(rotationSpeed)
                
                -- Yükseklik hesabı
                local yOffset
                if tornadoMode == "H" then
                    yOffset = 0 -- sabit yükseklik
                else -- V modu
                    yOffset = height * (math.abs(math.sin((pos.Y - center.Y) / height))) -- orijinal tornado dikey salınımı
                end
                
                local targetPos = Vector3.new(
                    center.X + math.cos(newAngle) * math.min(radius, distance),
                    center.Y + yOffset,
                    center.Z + math.sin(newAngle) * math.min(radius, distance)
                )
                local directionToTarget = (targetPos - part.Position).unit
                part.Velocity = directionToTarget * attractionStrength
            end
        end
    end
end

-- Kalp güncellemesi (H ve V seçeneğine göre)
local function heartUpdate()
    local humanoidRootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end
    local center = humanoidRootPart.Position
    local speed = 0.03
    
    for _, part in pairs(parts) do
        if part.Parent and not part.Anchored then
            local t = partAngles[part] or 0
            t = t + speed
            if t > 2 * math.pi then t = t - 2 * math.pi end
            partAngles[part] = t
            
            local scale = radius * 0.5
            local x = scale * 16 * math.sin(t)^3
            local z = scale * (13 * math.cos(t) - 5 * math.cos(2*t) - 2 * math.cos(3*t) - math.cos(4*t))
            local y = 0
            
            if heartMode == "V" then
                y = math.sin(t * 3) * height * 0.3
            end
            
            local targetPos = center + Vector3.new(x, y, z)
            local direction = (targetPos - part.Position).unit
            part.Velocity = direction * attractionStrength
        end
    end
end

RunService.Heartbeat:Connect(function()
    if not ringPartsEnabled then return end
    
    if currentMode == "tornado" then
        tornadoUpdate()
    elseif currentMode == "heart" then
        heartUpdate()
    end
end)

-- GUI buton işlevleri
ToggleButton.MouseButton1Click:Connect(function()
    ringPartsEnabled = not ringPartsEnabled
    ToggleButton.Text = ringPartsEnabled and "Ring Parts On" or "Ring Parts Off"
    ToggleButton.BackgroundColor3 = ringPartsEnabled and Color3.fromRGB(50, 205, 50) or Color3.fromRGB(30,30,30)
    playSound("12221967")
end)

DecreaseRadius.MouseButton1Click:Connect(function()
    radius = math.max(1, radius - 2)
    RadiusDisplay.Text = "Radius: " .. radius
    playSound("12221967")
end)

IncreaseRadius.MouseButton1Click:Connect(function()
    radius = math.min(1000, radius + 2)
    RadiusDisplay.Text = "Radius: " .. radius
    playSound("12221967")
end)

-- Mod seçim butonları
TornadoBtn.MouseButton1Click:Connect(function()
    currentMode = "tornado"
    TornadoBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
    HeartBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
    -- Tornado alt butonlarını güncelle
    if tornadoMode == "H" then
        TornadoH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
    else
        TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoV.BackgroundColor3 = Color3.fromRGB(50,205,50)
    end
    -- Heart alt butonlarını sönük yap (ama görünür kalsın)
    HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
    HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
    playSound("12221967")
end)

HeartBtn.MouseButton1Click:Connect(function()
    currentMode = "heart"
    HeartBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
    TornadoBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
    -- Heart alt butonlarını güncelle
    if heartMode == "H" then
        HeartH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
    else
        HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartV.BackgroundColor3 = Color3.fromRGB(50,205,50)
    end
    -- Tornado alt butonlarını sönük yap
    TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
    TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
    playSound("12221967")
end)

-- Tornado alt butonları
TornadoH.MouseButton1Click:Connect(function()
    if currentMode == "tornado" then
        tornadoMode = "H"
        TornadoH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    else
        -- Tornado modunda değilken tıklanırsa otomatik tornado moduna geç ve H seç
        currentMode = "tornado"
        tornadoMode = "H"
        TornadoBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    end
end)

TornadoV.MouseButton1Click:Connect(function()
    if currentMode == "tornado" then
        tornadoMode = "V"
        TornadoV.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    else
        currentMode = "tornado"
        tornadoMode = "V"
        TornadoBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoV.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    end
end)

-- Heart alt butonları
HeartH.MouseButton1Click:Connect(function()
    if currentMode == "heart" then
        heartMode = "H"
        HeartH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    else
        currentMode = "heart"
        heartMode = "H"
        HeartBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartH.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    end
end)

HeartV.MouseButton1Click:Connect(function()
    if currentMode == "heart" then
        heartMode = "V"
        HeartV.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    else
        currentMode = "heart"
        heartMode = "V"
        HeartBtn.BackgroundColor3 = Color3.fromRGB(50,205,50)
        TornadoBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
        HeartV.BackgroundColor3 = Color3.fromRGB(50,205,50)
        HeartH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoH.BackgroundColor3 = Color3.fromRGB(30,30,30)
        TornadoV.BackgroundColor3 = Color3.fromRGB(30,30,30)
        playSound("12221967")
    end
end)

--------------------------------------------------
-- BİLDİRİM VE CHAT
--------------------------------------------------
local function getThumbnail()
    local success, userId = pcall(function()
        return Players:GetUserIdFromNameAsync("31")
    end)
    
    if success then
        local content, isReady = Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
        return content
    end
    return ""
end

StarterGui:SetCore("SendNotification", {
    Title = "Super ring parts V4",
    Text = "enjoy",
    Icon = getThumbnail(),
    Duration = 5
})

local function SendChatMessage(message)
    if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
        local textChannel = TextChatService.TextChannels.RBXGeneral
        if textChannel then
            textChannel:SendAsync(message)
        end
    else
        local success = pcall(function()
            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents"):FindFirstChild("SayMessageRequest"):FireServer(message, "All")
        end)
    end
end

SendChatMessage("mert femboy")
